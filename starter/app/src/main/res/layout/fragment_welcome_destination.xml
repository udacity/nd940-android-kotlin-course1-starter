<?xml version="1.0" encoding="utf-8"?>
<!-- This page was generated by adding a new destination in main_navigation.xml
I chose to use a linear layout for this screen as the contents were simple.
Here were the given requirements for this page.
Create a new Welcome screen destination that includes:
    A new layout
    At least 2 textviews
    A navigation button with actions to navigate to the instructions screen
-->


<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".Welcome.welcome_destination">


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            tools:ignore="UselessParent">

            <TextView
                android:id="@+id/welcome_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="@string/welcome" />

            <TextView
                android:id="@+id/info_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/about_this_app" />

            <Button
                android:id="@+id/welcome_button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/welcome_button" />
        </LinearLayout>

    </FrameLayout>
</layout>